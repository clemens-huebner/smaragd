import { AutomationDegreePipe } from './automation-degree.pipe';

describe('AutomationDegreePipe', () => {
  it('create an instance', () => {
    const pipe = new AutomationDegreePipe();
    expect(pipe).toBeTruthy();
  });
});
