import { AutomationDegreeIconPipe } from './automation-degree-icon.pipe';

describe('AutomationDegreeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new AutomationDegreeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
